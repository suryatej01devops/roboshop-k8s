apiVersion: v1
kind: Pod 
metadata:
  name: nginx-pod
  # These labels are used to register this pod with labels
  labels: 
    project: roboshop
    component: frontend
    environment: dev
    purpose: load-balancer
spec:
  containers:
  - name: nginx-container
    image: nginx:latest

     
        